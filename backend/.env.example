# GitHub Packages Authentication
# Required for installing @jackdo69/job-tracker-shared-types from GitHub Packages
# Get token from: https://github.com/settings/tokens (with read:packages scope)
GITHUB_NPM_PUBLISH_TOKEN=your-github-token-here

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/jobtracker

# CORS Origins (comma-separated)
# NOTE: First origin is used for OAuth redirects to frontend
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# Security - CHANGE IN PRODUCTION!
SECRET_KEY=your-secret-key-change-this-in-production

# Google OAuth Configuration (Optional)
# Get credentials from: https://console.cloud.google.com/apis/credentials
# Leave empty to disable Google sign-in
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:8080/api/auth/google/callback

# Supabase Storage Configuration
# Get from: https://app.supabase.com -> Project Settings -> API
# IMPORTANT: Use service_role key (not anon key) for backend to bypass RLS
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Server
HOST=0.0.0.0
PORT=8080
